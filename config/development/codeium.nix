{
  plugins.codeium-vim = {
    enable = true;
  };

  # デフォルトでは以下のキーバインドが有効になります：
  # <Tab>   : 提案されたコードを確定 (Accept)
  # <C-]>   : 次の提案へ (Next)
  # <C-[>   : 前の提案へ (Prev)
  # <C-x>   : 提案を消去 (Clear)
}
